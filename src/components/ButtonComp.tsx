'use client';

import React from 'react';
import { Button } from '~/widgets';

const ButtonComp = () => {
    return (
        <div>
            <Button variant='solid'>Hehe</Button>
        </div>
    );
};

export default ButtonComp;